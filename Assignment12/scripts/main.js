

var button = getElementById('btnSubmit');button.addEventListener('click', submit);

var button2 = getElementById('btnReset');button2.addEventListener('click', reset);

var inputs = ['firstName', 'lastName', 'emailAddress', 'password', 'passwordConfirm']




function submit {

  if (firstName === "") {

    messages.push(First Name is Required)

  }


}
